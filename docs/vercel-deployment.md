# การ Deploy Frontend ไปยัง Vercel 🚀

เอกสารนี้จะแนะนำวิธีการนำส่วน Frontend (Next.js) ของโปรเจกต์ AI Privacy Guard ไปรันบน Vercel ซึ่งจะทำให้เว็บโหลดเร็วขึ้นและจัดการง่ายกว่า Render

## สิ่งที่ต้องเตรียม
1.  บัญชี **Vercel** (สมัครด้วย GitHub)
2.  URL ของ Backend ที่รันบน Render (เช่น `https://ai-privacy-guard-backend.onrender.com`) - **สำคัญมาก!**

---

## ขั้นตอนการติดตั้ง (Step-by-Step)

### 1. เชื่อมต่อ GitHub กับ Vercel
1.  ไปที่ [Vercel Dashboard](https://vercel.com/dashboard)
2.  คลิกปุ่ม **"Add New..."** -> **"Project"**
3.  เลือก Repository **`patcharaph/AI-Privacy-Guard`** แล้วกด **Import**

### 2. ตั้งค่า Project (Configure Project)
นี่คือจุดสำคัญที่ต้องตั้งค่าให้ถูก เพราะโปรเจกต์นี้มีทั้ง frontend และ backend ใน repo เดียวกัน

*   **Project Name**: ตั้งชื่อตามต้องการ (เช่น `ai-privacy-guard-web`)
*   **Framework Preset**: Vercel จะเลือก `Next.js` ให้อัตโนมัติ (ถ้าไม่เลือก ให้เลือกเอง)
*   **Root Directory**: ⚠️ **ต้องแก้ไข**
    *   คลิก **Edit** ที่ช่อง `Root Directory`
    *   เลือกโฟลเดอร์ **`frontend`**
    *   (เหตุผล: เพราะ code Next.js ของเราอยู่ในโฟลเดอร์นี้ ไม่ใช่ root หลัก)

### 3. ตั้งค่า Environment Variables
เราต้องบอก Frontend ว่า Backend อยู่ที่ไหน

1.  คลิกเมนู **Environment Variables**
2.  เพิ่มค่าดังนี้:
    *   **Key**: `NEXT_PUBLIC_API_URL`
    *   **Value**: ใส่ URL ของ Render Backend ของคุณ (ที่ลงท้ายด้วย `.onrender.com`)
    *   (อย่าใส่ `/api` ต่อท้าย ให้ใส่แค่ domain หลัก เช่น `https://my-app.onrender.com`)

### 4. Deploy 🚀
1.  กดปุ่ม **Deploy**
2.  รอประมาณ 1-2 นาที
3.  เมื่อเสร็จแล้ว หน้าเว็บจะเด้งพลุแสดงความยินดี (Congratulations!)

---

## การตรวจสอบ (Testing)
1.  เปิดหน้าเว็บที่ Vercel สร้างให้ (เช่น `https://ai-privacy-guard-web.vercel.app`)
2.  ลองกดอัปโหลดรูปภาพ
3.  **ถ้าใช้งานได้**: แสดงว่าเชื่อมต่อกับ Backend สำเร็จ
4.  **ถ้า Error (Network Error)**:
    *   เช็ค `NEXT_PUBLIC_API_URL` ใน Vercel Settings อีกครั้งว่าถูกต้องไหม
    *   เช็คว่า Backend บน Render ทำงานปกติ (Status: Healthy)

> [!TIP]
> **Domain Name**: หากคุณมีโดเมนส่วนตัว คุณสามารถไปผูกที่เมนู Settings -> Domains ใน Vercel ได้เลย ฟรีและมี HTTPS ให้ในตัว
